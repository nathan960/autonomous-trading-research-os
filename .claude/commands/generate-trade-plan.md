# Generate Trade Plan

Run Layer 3 Trade Plan Generation. Generate deterministic `trade_plan.json` only. Run `python scripts/generate_trade_plan.py --approve-paper`, verify all gates and no live trading, show diff, then commit/push or open PR.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
