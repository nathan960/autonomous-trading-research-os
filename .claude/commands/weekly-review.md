# Weekly Review

Run Layer 9 Weekly Review. Command: `python scripts/weekly_review.py`. Do not overfit. Summarize evidence, trigger outcomes, trades/skips, drawdown, and operational issues.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
