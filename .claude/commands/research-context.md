# Research Context

Run Layer 6 Research Context. Web/connectors are research-only and must not generate orders. Command: `python scripts/research_context.py`. Separate facts, hypotheses, and unknowns.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
