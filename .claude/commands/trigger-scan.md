# Trigger Scan

Run Layer 2 Trigger Scan. Do not use web or discretionary judgment. Run `python scripts/trigger_scan.py`, inspect trigger and signal logs, show diff, then commit/push or open PR.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
