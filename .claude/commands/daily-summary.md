# Daily Summary

Run Layer 8 Daily Summary. Command: `python scripts/daily_summary.py`. Summarize triggers, skips, plan status, execution status, risk gates, data quality, and position state.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
