# Position Monitor

Run Layer 5 Position Monitor. Reconcile against Alpaca paper state. Command: `python scripts/position_monitor.py --alpaca`. Never rely only on memory if broker state disagrees.

Always run compile checks first, never print secrets, never force-push, and preserve auditability.
